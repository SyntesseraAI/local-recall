{
  "name": "local-recall",
  "version": "0.1.0",
  "description": "A local markdown-powered memory system for AI coding assistants",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "local-recall": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && npm run build:scripts",
    "build:scripts": "mkdir -p dev-marketplace/local-recall-plugin/scripts/hooks dev-marketplace/local-recall-plugin/scripts/mcp-server && esbuild src/hooks/session-start.ts src/hooks/stop.ts src/hooks/user-prompt-submit.ts src/hooks/user-prompt-submit-thinking.ts --bundle --platform=node --format=esm --outdir=dev-marketplace/local-recall-plugin/scripts/hooks --external:better-sqlite3 --external:sqlite-vec --external:fastembed --external:onnxruntime-node --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\" && esbuild src/mcp-server/server.ts --bundle --platform=node --format=esm --outdir=dev-marketplace/local-recall-plugin/scripts/mcp-server --external:@modelcontextprotocol/sdk --external:better-sqlite3 --external:sqlite-vec --external:fastembed --external:onnxruntime-node --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\"",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:unit": "vitest run --dir src",
    "test:integration": "vitest run --dir tests/integration",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "mcp:start": "node dist/mcp-server/server.js",
    "hooks:session-start": "node dist/hooks/session-start.js",
    "hooks:stop": "node dist/hooks/stop.js",
    "hooks:user-prompt-submit": "node dist/hooks/user-prompt-submit.js",
    "hooks:user-prompt-submit-thinking": "node dist/hooks/user-prompt-submit-thinking.js",
    "config:validate": "node dist/cli.js config:validate"
  },
  "keywords": [
    "ai",
    "memory",
    "claude",
    "mcp",
    "markdown",
    "context",
    "coding-assistant"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "^12.5.0",
    "fastembed": "^2.0.0",
    "gray-matter": "^4.0.3",
    "sqlite-vec": "^0.1.7-alpha.2",
    "uuid": "^10.0.0",
    "yaml": "^2.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "esbuild": "^0.27.0",
    "eslint": "^9.0.0",
    "typescript": "^5.9.0",
    "vitest": "^2.0.0"
  }
}
