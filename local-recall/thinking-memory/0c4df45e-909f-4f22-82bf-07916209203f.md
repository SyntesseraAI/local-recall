---
id: 0c4df45e-909f-4f22-82bf-07916209203f
subject: >-
  I see - `episodicEnabled` defaults to `false` and `thinkingEnabled` defaults
  to `true`. The user...
applies_to: global
occurred_at: '2025-12-02T22:58:51.256Z'
content_hash: 5c53c130612106cf
---
## Thought

I see - `episodicEnabled` defaults to `false` and `thinkingEnabled` defaults to `true`. The user mentioned they wanted to remove an env var from the MCP config. Let me check the .claude/settings.json to see the current configuration.

## Output

I see - `episodicEnabled` already defaults to `false`, and `thinkingEnabled` defaults to `true`. Let me check the current MCP settings:
