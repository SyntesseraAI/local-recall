---
id: 21c2e513-7e7b-4577-ba4f-569d1bfa29a2
subject: >-
  IndexManager.getIndex() ensures .gitignore file is created on every session
  start
keywords:
  - gitignore
  - session-start
  - index-manager
  - initialization
  - startup
applies_to: 'file:src/core/index.ts'
occurred_at: '2025-12-21T19:29:28.992Z'
content_hash: d226cae2e19c2df0
---
The `IndexManager.getIndex()` method now calls `ensureGitignore()` on every invocation (line 99), which means the `.gitignore` file is automatically created during session start when the session-start hook calls `searchEngine.getRelevantForSession()` → `indexManager.getIndex()`. This ensures the `.gitignore` exists even if the index.json already exists or if it's the first session.

The `.gitignore` file ignores:
- `orama-*-index.json` (vector indexes)
- `recall.log` (debug logs)

This approach leverages the existing startup flow rather than requiring a separate .gitignore creation hook.
